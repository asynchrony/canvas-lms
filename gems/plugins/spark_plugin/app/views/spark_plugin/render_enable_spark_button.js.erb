require([
  'jquery' /* $ */
], function($) {
  $('#enable_spark_button').html("<%= escape_javascript(render partial: 'enable_spark_button', locals: {can_enable: @can_enable, course_code: @course_code, course_id: @course_id} ) %>");
});
